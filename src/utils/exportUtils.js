
export const exportToExcel = (data, type = 'data') => {
  console.log(`Exportando ${type} a Excel:`, data);
  alert(`Esta función exportará ${data.length} registros en la versión final de la aplicación`);
};